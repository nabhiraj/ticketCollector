import { View } from "react-native";

export default function EventManager({eventInfo}:any){
    //we will have to fix the type of the event list and event item which i have created
    return (
        <View>
            
        </View>
    );
}